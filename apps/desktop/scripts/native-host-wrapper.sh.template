#!/usr/bin/env bash
# Generated by register-*.sh — Chromium launches this as the native messaging host.
set -euo pipefail
export LANGUAGE_LLM_NATIVE=1
DIR="$(cd "$(dirname "$0")" && pwd)"
exec "$DIR/language-llm-desktop" --native-messaging "$@"
