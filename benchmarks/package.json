{
  "name": "@language-llm/benchmarks",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "ASR, MT, subtitle, ambiguity, privacy, and accessibility evaluation harnesses",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@language-llm/protocol": "workspace:*",
    "@language-llm/lyrics": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
